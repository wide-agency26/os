import { Workspace } from "@/components/frappe-ui/Workspace";

export function FoundersOnly() {
  return (
    <Workspace>
      <p className="text-sm text-text-secondary">Founders only.</p>
    </Workspace>
  );
}
